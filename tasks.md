does this codebase proactively do all of the below things?

- run multiple agents in parallel in sandboxed environments in background and reports back the progress. it can create agents or we can ask it to. can run those agents periodically or forever.

- communicates via slack and whatsapp
- can send me a text via slack/whatsapp on its own if it needs to. doesnt need to be triggered by me.
- is always online and always available
- chat is never blocked by other processes
- i can queue messages for it to do

- remembers everything and recalls relavent info before every response

- updates its own system prompt everynight to grow
- observes my actions and proactively creates new rules, skills, tools, policies and code to improve itself

- creates new skills and write new tools for itself whenever it needs it

- it itself runs in a sandboxed environment
- runs all code in a sandboxed environment
- edits its own code if theres a bug, or some feature is missing
- parses ast to edit code
- runs tests to test itself and any other code it writes
- tracks its own upgrades in git and can rollback

- logs everything that it does and monitors its own logs

- has a universal realtime webhook that catches all incoming events

- has its own prioritization queue

- all ai communication goes through opencode's default model

- browses the web (by opening up my browser in which everything is logged in) and does deep web research
- solves any captchas

- has its own task board in which i can add tasks and go to sleep to find it has done everything in the morning

- has all permissions so that it can run without asking me any permissions

- has a failsafe kill switch that i can use to stop it